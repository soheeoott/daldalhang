<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ver01.mappers.personalMapper"> <!-- mapper의 이름표 -->
	
	<select id ="selectList" resultType="vo.PersonalVO"> 
		select id, password, email from personal order by id
	</select>

	<select id = "selectOne" resultType="vo.PersonalVO">
		select * from personal where id = #{id}
	</select>
	
	<insert id = "insert" parameterType="vo.PersonalVO">
		insert into personal (id, password, email) values (#{id}, #{password}, #{email})
	</insert>
	
	<select id= "IdCheck" parameterType="String" resultType="int">
		select count(*) from personal where id = #{id}
	</select>
	
</mapper>